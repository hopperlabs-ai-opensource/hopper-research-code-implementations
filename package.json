{
  "name": "hopper-research-code-implementations",
  "version": "0.5.1",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "test": "node --test packages/*/*.test.mjs",
    "build": "node scripts/build.mjs",
    "check": "npm test && npm run build",
    "start": "npm run build && node scripts/serve.mjs",
    "mcp": "node scripts/cli.mjs mcp",
    "doctor": "node scripts/cli.mjs doctor",
    "cli": "node scripts/cli.mjs"
  },
  "bin": {
    "hopper-research": "scripts/cli.mjs"
  },
  "files": [
    "scripts",
    "packages",
    "vendor",
    "dist",
    "README.md",
    "LICENSE",
    "docs"
  ],
  "engines": {
    "node": ">=22"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/hopperlabs-ai-opensource/hopper-research-code-implementations.git"
  }
}
